@page
@model OdonChat.Pages.ChatModel
@{
    ViewData["Title"] = "Chat";
}
@section CSS {
    <link rel="stylesheet" href="css/chat.css">
}

<div class="app-container">
    <header class="header">
        <div class="header-title">OdonChat</div>
        <div class="dropdown input-group">
            <input type="text" class="form-control" id="searchInput" placeholder="Cerca...">
            <button id="searchButton" class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">Cerca</button>
            <ul class="dropdown-menu" id="searchResults" style="width: 100%;">
            </ul>
        </div>
        <a class="btn btn-secondary dropdown-toggle" type="button" id="navButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <span>@User?.FindFirst("username")?.Value</span>
            <img src="img/avatar.png" class="avatar"></img>
        </a>
        <ul class="dropdown-menu" aria-labelledby="navButton">
            <li><a class="dropdown-item" asp-page="Account">Account</a></li>
            <li><a class="dropdown-item" asp-page="Index" asp-page-handler="Logout">Logout</a></li>
        </ul>
    </header>

    <div class="main-content">
        <aside class="sidebar">
            <!-- to remove later
            <div class="sidebar-user-card">
                <div class="avatar"></div>
                <div class="user-info">
                    <span class="user-name">Nome</span>
                    <span class="user-message">Messaggio</span>
                </div>
            </div>
            -->
        </aside>

        <main class="chat-area">
            <div class="message-input-container">
                <input type="text" class="message-input" placeholder="Scrivi un messaggio...">
                <button class="send-button">Invio</button>
            </div>
        </main>
    </div>
</div>

@section JS{
    <script src="~/js/signalr/dist/browser/signalr.js"></script>
    <script src="~/js/chat.js"></script>
}